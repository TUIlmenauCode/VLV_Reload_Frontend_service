const db = require("../../dbConnection");
const express = require("express");
var router = express.Router();

router.post("/publicCalendarDataForGroup", function(req, res, next){

});

module.exports = router;